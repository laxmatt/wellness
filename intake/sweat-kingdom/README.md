# Sweat Kingdom, Awin feed F3219

`awin-125462-f3219-2026-09-13.csv` is the feed as downloaded on 2026-09-13,
byte for byte. It is kept because it is the provenance: every record derived
from it can be traced to a row, and a later feed can be diffed against this one.

**It carries no credential.** The file was checked for key-, token-, secret- and
password-shaped strings and holds none. The only identifiers in it are
`awinmid=125462` (the advertiser) and `awinaffid=3090899` (the publisher), which
appear in every affiliate link Awin issues and are not secret. The URL the feed
was downloaded from does carry a key; that URL is not recorded here and must not
be committed.

225 rows, 62 columns, 47 of them empty in every row.
